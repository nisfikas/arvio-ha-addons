name: "Arvio Agent"
version: "0.1.13"
slug: arvio_agent
description: "Arvio hub agent — local UI, claim, outbound cloud enroll + remote relay."
url: "https://github.com/nisfikas/arvio-ha-addons"
arch:
  - aarch64
  - amd64
  - armv7
init: false
startup: application
boot: auto
homeassistant_api: true
hassio_api: true
hassio_role: default
host_network: false
stage: experimental
webui: "http://[HOST]:[PORT:8099]/"
watchdog: "http://[HOST]:[PORT:8099]/health"
ports:
  8099/tcp: 8099
ports_description:
  8099/tcp: "Arvio local UI and agent API"
options:
  cloud_url: "https://cloud.arvio.systems"
  serial: ""
  relay_url: "https://cloud.arvio.systems"
  relay_token: "lab-relay-token"
schema:
  cloud_url: str
  serial: str?
  relay_url: str?
  relay_token: str?
